{
  "name": "cockpit-terminal",
  "version": "0.1.0",
  "private": true,
  "description": "The wmux cockpit mockup turned into a real, functional PowerShell terminal (standalone, no Electron).",
  "main": "server.cjs",
  "scripts": {
    "start": "node server.cjs"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-search": "^0.16.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  }
}
